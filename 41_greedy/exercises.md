# Chapter 41: Greedy Algorithms - Exercises

## Instructions

Try to solve each problem by identifying the greedy choice. Solutions in `solutions.md`.

---

## Easy Problems

### E1: Assign Cookies (LeetCode 455)
```python
def find_content_children(g: List[int], s: List[int]) -> int:
    """Maximize satisfied children. g = greed, s = cookie sizes."""
    pass
```

### E2: Lemonade Change (LeetCode 860)
```python
def lemonade_change(bills: List[int]) -> bool:
    """Can provide correct change for all customers? Bills are 5, 10, or 20."""
    pass
```

### E3: Best Time to Buy and Sell Stock II (LeetCode 122)
```python
def max_profit(prices: List[int]) -> int:
    """Max profit with multiple transactions (no overlapping)."""
    pass
```

### E4: Maximum Subarray (LeetCode 53)
```python
def max_subarray(nums: List[int]) -> int:
    """Find maximum sum subarray."""
    pass
```

### E5: Can Place Flowers (LeetCode 605)
```python
def can_place_flowers(flowerbed: List[int], n: int) -> bool:
    """Can plant n flowers without adjacency?"""
    pass
```

---

## Medium Problems

### M1: Jump Game (LeetCode 55)
```python
def can_jump(nums: List[int]) -> bool:
    """Can reach last index?"""
    pass
```

### M2: Jump Game II (LeetCode 45)
```python
def jump(nums: List[int]) -> int:
    """Minimum jumps to reach last index."""
    pass
```

### M3: Gas Station (LeetCode 134)
```python
def can_complete_circuit(gas: List[int], cost: List[int]) -> int:
    """Starting station for circular route."""
    pass
```

### M4: Non-overlapping Intervals (LeetCode 435)
```python
def erase_overlap_intervals(intervals: List[List[int]]) -> int:
    """Minimum intervals to remove to make non-overlapping."""
    pass
```

### M5: Partition Labels (LeetCode 763)
```python
def partition_labels(s: str) -> List[int]:
    """Partition into max parts."""
    pass
```

### M6: Task Scheduler (LeetCode 621)
```python
def least_interval(tasks: List[str], n: int) -> int:
    """Minimum time with cooldown n."""
    pass
```

### M7: Meeting Rooms II (LeetCode 253)
```python
def min_meeting_rooms(intervals: List[List[int]]) -> int:
    """Minimum rooms needed."""
    pass
```

### M8: Boats to Save People (LeetCode 881)
```python
def num_rescue_boats(people: List[int], limit: int) -> int:
    """Minimum boats (max 2 people per boat)."""
    pass
```

### M9: Remove K Digits (LeetCode 402)
```python
def remove_k_digits(num: str, k: int) -> str:
    """Remove k digits to make smallest number."""
    pass
```

### M10: Wiggle Subsequence (LeetCode 376)
```python
def wiggle_max_length(nums: List[int]) -> int:
    """Maximum wiggle subsequence length."""
    pass
```

---

## Hard Problems

### H1: Candy (LeetCode 135)
```python
def candy(ratings: List[int]) -> int:
    """Minimum candies (each child >= 1, higher rating gets more than neighbors)."""
    pass
```

### H2: Create Maximum Number (LeetCode 321)
```python
def max_number(nums1: List[int], nums2: List[int], k: int) -> List[int]:
    """Create max number of length k from two arrays."""
    pass
```

### H3: IPO (LeetCode 502)
```python
def find_maximized_capital(k: int, w: int, profits: List[int], capital: List[int]) -> int:
    """Maximize capital after k projects."""
    pass
```

### H4: Minimum Number of Arrows (LeetCode 452)
```python
def find_min_arrow_shots(points: List[List[int]]) -> int:
    """Minimum arrows to burst all balloons."""
    pass
```

### H5: Split Array into Consecutive Subsequences (LeetCode 659)
```python
def is_possible(nums: List[int]) -> bool:
    """Can split into consecutive sequences of length >= 3?"""
    pass
```

Good luck!
